#include <stdio.h>
 
int main(){
    int i;
    double N[100], n;
    scanf("%lf", &n);
    for(i = 0; i < 100; i++){
        if (i == 0){
            N[i] = n;
        }else{
            N[i] = n / 2;
            n = n / 2;
        }
     printf("N[%d] = %.4lf\n", i, N[i]);
    }
return 0;
}
