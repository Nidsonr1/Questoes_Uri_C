#include <stdio.h>
int main(){
    int  T, cont, i = 0, vetor[1000];
        scanf("%d", &T);
        for(cont = 0; cont < 1000; cont++){
            printf("N[%d] = %d\n", cont, i);
            i++;
        if(i == T){
            i = 0;
        }
    }
return 0;
}
